
//se abre el archivo desde el main

/*void guardarPartida(Grafo *grafo, jugador *jugadorActual, Node *nodoActual, const char *nombreArchivo) {
  FILE *archivo = fopen(nombreArchivo, "w");
  
  // Guardar datos del jugador
  fprintf(archivo, "Jugador:\n");
  fprintf(archivo, "Nombre: %s\n", jugadorActual->nombre);
  fprintf(archivo, "Fuerza: %d\n", jugadorActual->stats.fuerza);
  fprintf(archivo, "Salud: %d\n", jugadorActual->stats.salud);
  fprintf(archivo, "Inventario:\n");
  for (int i = 0; i < 2; i++) {
    if (strcmp(jugadorActual->inventario[i].item, "") != 0) {
      fprintf(archivo, "- %s\n", jugadorActual->inventario[i].item);
    }
  }
  
  // Guardar datos del nodo actual
  fprintf(archivo, "\nNodo actual:\n");
  fprintf(archivo, "ID: %s\n", nodoActual->ID);
  fprintf(archivo, "Tipo de historia: %s\n", nodoActual->TipoHistoria);
  // Guardar más datos del nodo según sea necesario
  
  fclose(archivo);
}
*/